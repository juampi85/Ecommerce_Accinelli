import styled from "styled-components";

export const CartWidgetStyle = styled.button `
  color: #ebc88b;
  background-color: #49426c;
  border: none;
  &:hover {
    transform: scale(1.3);
    transition: 0.4s ease-in;
  }
`